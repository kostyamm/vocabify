import { Fragment } from 'react';
import { Button } from '@mui/material'

export const Home = () => {
    return (
        <Fragment>
            <Button variant="outlined">Button</Button>
        </Fragment>
    );
};
